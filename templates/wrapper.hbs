(function(root, factory) {
    if(typeof define === 'function' && define.amd) {
        define({{#if amdName}}'{{amdName}}', {{/if}}[{{{dependencyPaths}}}], factory);
    } else {
        {{#if globalName}}root.{{globalName}} = factory({{{dependencyNames}}});{{else}}factory({{{dependencyNames}}});{{/if}}
    }
}(this, {{{factorySrc}}}));
