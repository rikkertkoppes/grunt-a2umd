(function(root, factory) {
    if(typeof define === 'function' && define.amd) {
        define({{#if amdName}}'{{amdName}}', {{/if}}[{{{dependencyPaths}}}], factory);
    }
    else {
        root.{{globalName}} = factory({{{dependencyNames}}});
    }
}(this, {{{factorySrc}}}));